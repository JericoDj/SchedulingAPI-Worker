{
  "name": "scheduler-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Local worker that triggers backend scheduler endpoint on an interval.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
